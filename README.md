# WonderWp Plugin Skeleton

A WonderWp package containing many abstract classes and traits to help you define a plugin structure.

This structure is used by default when a plugin is created by the generator.
